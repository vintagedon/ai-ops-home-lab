# --- Domain Configuration ---
# The public-facing URL for your Vaultwarden instance.
# This MUST start with https://
VW_DOMAIN=https://vw.radioastronomy.io

# --- Admin Token ---
# IMPORTANT: Generate a strong, random password to secure the admin panel.
# You can use a password manager or run: openssl rand -base64 48
VW_ADMIN_TOKEN=YOUR_SUPER_SECRET_ADMIN_TOKEN_HERE

# --- Host Port Mapping ---
# The port on the Docker host that will map to the container's port 80.
# Choose a port that is not already in use.
VW_HTTP_PORT=8081

# --- Docker Networking & Timezone ---
DOCKER_NETWORK=vaultwarden_network
TZ=America/New_York
