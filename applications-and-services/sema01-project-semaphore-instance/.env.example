# --- Host Path for Persistent Data ---
# IMPORTANT: Change this to the absolute path on your Docker host.
SEMAPHORE_DATA_PATH=/path/on/host/to/semaphore-data

# --- Port Mapping ---
SEMAPHORE_HTTP_PORT=3001 # Using 3001 to avoid conflict with Gitea

# --- PostgreSQL Database Connection ---
SEMAPHORE_DB_HOST=10.25.20.16
SEMAPHORE_DB_PORT=5432
SEMAPHORE_DB_USER=semaphore
SEMAPHORE_DB_PASSWORD=YOUR_STRONG_SEMAPHORE_PASSWORD
SEMAPHORE_DB_NAME=semaphore

# --- Local Admin User (Fallback) ---
SEMAPHORE_ADMIN_NAME=Local Admin
SEMAPHORE_ADMIN_EMAIL=admin@semaphore.local
SEMAPHORE_ADMIN_USER=admin
SEMAPHORE_ADMIN_PASSWORD=YOUR_STRONG_ADMIN_PASSWORD

# --- Security ---
# Replace with a new key generated by: openssl rand -base64 32
SEMAPHORE_ENCRYPTION_KEY=gs72mPntFATGJs9qK0pQ0rKtfidlexiMjYCH9gWKhTU=

# --- Active Directory LDAP Integration ---
SEMAPHORE_LDAP_HOST=10.25.20.2
SEMAPHORE_LDAP_PORT=636
SEMAPHORE_LDAP_BIND_USER=adbinduser@radioastronomy.io
SEMAPHORE_LDAP_BIND_PASSWORD=Use-Hunt-Encourage-Birth-1
# Based on your AD screenshot, this is the correct search DN for privileged users.
SEMAPHORE_LDAP_SEARCH_DN="OU=Privileged,OU=Users,OU=ORG,DC=radioastronomy,DC=io"

# --- Docker Networking & Timezone ---
DOCKER_NETWORK=semaphore_network
TZ=America/New_York
